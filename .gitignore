# JARs de Flink — se descargan automáticamente con post-init-cluster
flink-jars/*.jar

# Archivos compilados de Python
__pycache__/
*.pyc

# AWS cluster — archivos generados en tiempo de ejecución (credenciales y estado)
aws-hadoop-cluster/*.pem
aws-hadoop-cluster/cluster-state.env
